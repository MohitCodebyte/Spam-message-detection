# Spam-message-detection
This project is a Spam Message Classifier that identifies whether a message is Spam or Ham (Not Spam) using Machine Learning techniques.

The model is trained on a dataset containing labeled messages and uses Natural Language Processing (NLP) to convert text into numerical features. A Naive Bayes algorithm is applied for classification.
